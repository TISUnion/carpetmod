--- a/net/minecraft/util/ClassInheritanceMultiMap.java
+++ b/net/minecraft/util/ClassInheritanceMultiMap.java
@@ -4,23 +4,22 @@
 import com.google.common.collect.Lists;
 import com.google.common.collect.Maps;
 import com.google.common.collect.Sets;
-import java.util.AbstractSet;
-import java.util.Collections;
-import java.util.Iterator;
-import java.util.List;
-import java.util.Map;
-import java.util.Set;
+import me.jellysquid.mods.lithium.config.LiConfig;
 
+import java.util.*;
+import java.util.stream.Collectors;
+
 public class ClassInheritanceMultiMap<T> extends AbstractSet<T>
 {
     private static final Set < Class<? >> ALL_KNOWN = Sets.newHashSet();
-    private final Map < Class<?>, List<T >> map = Maps.newHashMap();
-    private final Set < Class<? >> knownKeys = Sets.newIdentityHashSet();
-    private final Class<T> baseClass;
-    private final List<T> values = Lists.newArrayList();
+    protected final Map < Class<?>, List<T >> map = Maps.newHashMap();
+    private final Set < Class<? >> knownKeys;
+    protected final Class<T> baseClass;
+    protected final List<T> values = Lists.newArrayList();
 
     public ClassInheritanceMultiMap(Class<T> baseClassIn)
     {
+        this.knownKeys = Sets.newIdentityHashSet();
         this.baseClass = baseClassIn;
         this.knownKeys.add(baseClassIn);
         this.map.put(baseClassIn, this.values);
@@ -31,6 +30,14 @@
         }
     }
 
+    protected ClassInheritanceMultiMap(Class<T> baseClassIn, boolean b)
+    //Only reachable by LithiumClassInheritanceMultiMap
+    {
+        this.knownKeys = null;
+        this.baseClass = baseClassIn;
+        this.map.put(baseClass, values);
+    }
+
     protected void createLookup(Class<?> clazz)
     {
         ALL_KNOWN.add(clazz);
